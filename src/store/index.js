import { createStore } from 'vuex'
import moduleServer from './server'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },

  modules: {
    server: moduleServer
  }
})
